prog {
  typedef PRat = rat *;
  typedef PRat2 = PRat;
  const a = 5;
  PRat2 x = (new rat);
  *x = [6/a];
  typedef PInt = int *;
  PInt y = (new int);
  *y = (a+1);
  *x + = [3/2];
  while (*y < 10) {
    print ([a/*y] * *x);
    *y + = 1;
  }
}
